#!/bin/bash
# Generated by WatchCow
{{if .HasRedirect}}
# Create CGI script for redirect mode
cat > "${TRIM_APPDEST}/ui/index.cgi" << 'EOFCGI'
#!/bin/bash
exec "{{.WatchCowAppDest}}/watchcow" --mode cgi --socket "{{.WatchCowPkgVar}}/watchcow.sock"
EOFCGI
chmod +x "${TRIM_APPDEST}/ui/index.cgi"
{{end}}
exit 0
